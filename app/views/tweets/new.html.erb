
	 <%= javascript_include_tag :juggernaut %>
	 <%= juggernaut :debug=> false %>

<h3>New message</h3>
<hr />

  <%= form_remote_tag(
            :url => { :action => :send_data },
            :complete => "$('message').value = ''" ) %>
        <%= text_area_tag( 'message', '', {:rows => 10, :cols => 50, :id => 'message'} ) %>
        <br />
        <table><tr><td>
        <%= submit_tag "Send Message", :class=>"message_button" %></td>
        <td><span class="menu_button"><%= link_to 'Show Past Messages', :action=>'index' %></span></td>
        </tr>
        </table>
      </form>
      <ul id="message_data" style="list-style:none"></ul>

<%= link_to 'Back', '/tweets/index' %>
